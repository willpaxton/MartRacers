# MartRacers

Use `npm install` to add all packages to your local machine.

Use `node index.js` to run the application.

This demo app is based off of the one hosted at https://socket.io/docs/v4/tutorial/[https://socket.io/docs/v4/tutorial/] and was used as a way to learn Socket.IO instructions. 